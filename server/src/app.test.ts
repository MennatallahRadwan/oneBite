import {describe,it,expect} from 'vitest';import request from 'supertest';import {createApp} from './app.js';describe('availability',()=>{it('uses combined cart points',async()=>{const r=await request(createApp()).post('/api/v1/availability/earliest').send({area:'Salmiya',items:[{slug:'chocolate-truffle-cake',quantity:2}]});expect(r.status).toBe(200);expect(r.body.capacityPoints).toBe(16)})});
